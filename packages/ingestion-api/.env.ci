# =============================================================================
# CI Environment Variables
# =============================================================================
# This file is used for GitHub Actions CI testing.
# The ADMIN_API_KEY and api_key are set from GitHub Secrets for security.

# Hurl test variables (base_url is localhost in CI)
base_url=http://localhost:8787
# Note: api_key is set from secrets.ADMIN_API_KEY in the workflow
