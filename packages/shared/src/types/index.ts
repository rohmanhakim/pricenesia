// =============================================================================
// Pricenesia Shared Types
// =============================================================================

// Placeholder for shared types
// Will be populated with database entity types as the project evolves

export interface HealthResponse {
  status: 'ok' | 'error'
  timestamp: string
  version?: string
}